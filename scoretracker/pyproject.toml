[project]
name = "scoretracker"
version = "0.1.0"
description = "Flask app to track user scores"
requires-python = ">=3.8"
dependencies = [
    "flask>=2.3.0",
    "gunicorn>=21.2.0",
    "pytz>=2023.3",
]

[tool.uv]
dev-dependencies = []
